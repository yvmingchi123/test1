spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://127.0.0.1:3306/test
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    port: 6379
    jedis:
      pool:
        max-active: 3000
        max-idle: 8
        min-idle: 0
    host: 127.0.0.1

mybatis:
  mapper-locations: classpath:/mappers/*/*Mapper.xml
  type-aliases-package: cn.bdqn.bdqnproject
server:
  port: 8080


